divide(n, d)={
  return = div(
    n=n,
    d=d
  )
}

/*
This is a test main program.
*/
main()={
  pre=all(
    hasConsoleOutput(
      this_thread
    ),
    hasConsoleInput(this_thread)
  )
  do=sequence(
    a1=print(divide(n13, n2)),
    a2=print(divide(f13, f2))
  )
  post=isEmpty(memory)
}
